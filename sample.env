# --------- Basic Configuration ---------
# Options are "production", "staging", "dev"
ENV_SETTING=dev

# --------- Database Configuration ---------
# For local development: postgresql+asyncpg://username:password@localhost:5432/url_shortener
# For Docker: postgresql+asyncpg://urlshortener:urlshortener@postgres:5432/url_shortener
PG_DSN=postgresql+asyncpg://urlshortener:urlshortener@postgres:5432/url_shortener

# --------- Redis Configuration ---------
# For local development: redis://localhost:6379/0
# For Docker: redis://redis:6379/0
REDIS_URL=redis://redis:6379/0

# --------- RabbitMQ Configuration ---------
# For local development: amqp://guest:guest@localhost:5672/
# For Docker: amqp://guest:guest@rabbitmq:5672/
RABBITMQ_URL=amqp://guest:guest@rabbitmq:5672/
RABBITMQ_EXCHANGE=url_shortener
RABBITMQ_QUEUE=click_events

# --------- Worker ID Configuration ---------
WORKER_ID_LEASE_TTL=60
WORKER_ID_RENEWAL_INTERVAL=30
MAX_WORKER_ID=1023

# --------- Application Configuration ---------
BASE_URL=http://localhost:8000

# --------- Database Connection Pool Configuration ---------
DB_POOL_SIZE=20
DB_MAX_OVERFLOW=10
DB_POOL_TIMEOUT=30
DB_POOL_RECYCLE=3600
DB_ECHO=false

